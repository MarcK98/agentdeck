{
  "name": "spawn-mobile",
  "version": "0.1.0",
  "description": "Spawn mobile — Expo client of the Spawn daemon via the relay. Runs in Expo Go today (npx expo start); TestFlight + push once Apple/Supabase accounts land.",
  "main": "index.ts",
  "private": true,
  "scripts": {
    "start": "expo start",
    "ios": "expo run:ios",
    "typecheck": "tsc --noEmit",
    "android": "expo run:android"
  },
  "dependencies": {
    "expo": "~54.0.0",
    "expo-asset": "~12.0.13",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5"
  },
  "devDependencies": {
    "@babel/core": "^7.25.0",
    "@types/react": "~19.1.0",
    "typescript": "^5.3.3"
  }
}
