node_modules/
.env
sessions.json
usage.jsonl
projects.json
*.log
.DS_Store

# Trello sync poll cursor (runtime state)
.trello-state.json

# AgentDeck desktop / monorepo
agentdeck.db
agentdeck.db-*
packages/desktop/dist/
agentdeck-daemon.token
agentdeck-daemon-sessions.json
# AES key for encrypted MCP secrets — NEVER commit (loses/leaks all stored tokens)
agentdeck-secret.key
# Per-ticket attachment files live under the data dir
ticket-files/
# Per-ticket git worktrees (default SPAWN_WORKTREES_DIR under the data dir)
worktrees/
agentdeck-daemon.pid
# Legacy pre-rebrand runtime files (kept ignored during migration)
spawn.db
spawn.db-*
spawn-daemon.token
spawn-daemon-sessions.json
spawn-secret.key
spawn-daemon.pid
# Expo/mobile artifacts
.expo/
packages/mobile/dist/
.railway-token
# Expo CNG native output (regenerated by prebuild / EAS)
packages/mobile/ios/
packages/mobile/android/
# Apple/ASC secrets (never commit)
.credentials/
credentials.json

# local .env backups (secrets) — never commit
.env.bak.*
