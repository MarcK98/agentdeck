node_modules/
.env
sessions.json
usage.jsonl
projects.json
*.log
.DS_Store

# Trello sync poll cursor (runtime state)
.trello-state.json

# Spawn desktop / monorepo
spawn.db
spawn.db-*
packages/desktop/dist/
spawn-daemon.token
spawn-daemon-sessions.json
# Per-ticket git worktrees (default SPAWN_WORKTREES_DIR under the data dir)
worktrees/
spawn-daemon.pid
